<?php
$form = $this->loginForm;
$form->prepare();
$form->setAttribute('action', $this->url('zfcuser/login', array('redirect' => '/')));
$form->setAttribute('method', 'post');
$form->setAttribute('autocomplete', 'off');
?>
<div class="ui three column centered grid">
    <div class="column">
        <h1 class="ui image header">
            <div class="content">
                <?php echo $this->translate('Prijavi se:'); ?>
            </div>
        </h1>
        <div class="ui medium form" _lpchecked="1">
        <?php echo $this->form()->openTag($form) ?>
        <?php echo $this->formElementErrors($form->get('identity')) ?>
            <div class="ui stacked segment">
                <div class="field">
                    <div class="ui left icon input">
                        <i class="user icon"></i>
                        <?php echo $this->formInput($form->get('identity')) ?>
                    </div>
                </div>
                <div class="field">
                    <div class="ui left icon input">
                        <i class="lock icon"></i>
                        <?php echo $this->formInput($form->get('credential')) ?>
                    </div>
                </div>
                <?php if ($this->redirect): ?>
                    <input type="hidden" name="redirect" value="<?php echo $this->escapeHtml($this->redirect) ?>" />
                <?php endif ?>
                <?php echo $this->formButton($form->get('submit')->setAttribute('class', 'ui fluid large submit button')) ?>
            </div>
            <div class="ui error message">hello</div>
            <?php echo $this->form()->closeTag() ?>
        </div>

        <div class="ui message">
            New to us?
            <?php if ($this->enableRegistration) : ?>
                <?php echo $this->translate('Niste registrirani?'); ?> <a href="<?php echo $this->url('zfcuser/register') . ($this->redirect ? '?redirect='.$this->escapeUrl($this->redirect) : '') ?>"><?php echo $this->translate('Registrirajte se!'); ?></a>
            <?php endif; ?>
        </div>
    </div>

</div>

